# Normalize line endings to LF in the repo for all text files,
# regardless of the OS the contributor is on.
* text=auto eol=lf

# Treat images as binary so they are never line-ending normalized.
*.png binary
